name: Deploy Web

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Trigger Cloudflare Pages Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Cloudflare Pages runs its own build (pnpm expo export -p web)
      # using env vars already configured in the Cloudflare Pages dashboard.
      # The _cf_routes.json file is copied into the dist/ via the build command
      # (cp _cf_routes.json dist/_routes.json) configured in Cloudflare Pages settings.

