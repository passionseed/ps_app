import { ValidationInput, ValidationIssue } from '../index';

export async function checkContentRelevance(input: ValidationInput): Promise<ValidationIssue[]> {
  // TODO: Implement content relevance check
  // Verify content matches topic and expert's field
  return [];
}
