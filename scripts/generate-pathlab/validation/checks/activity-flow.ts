import { ValidationInput, ValidationIssue } from '../index';

export async function checkActivityFlow(input: ValidationInput): Promise<ValidationIssue[]> {
  // TODO: Implement activity flow check
  // Verify activities progress logically
  return [];
}
